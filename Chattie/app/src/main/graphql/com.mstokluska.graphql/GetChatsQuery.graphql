query getChatsForUser($userId: ID!){
    getChatForUser(userId: $userId){
        id
        creator{
            name
            username
            id
        }
        recipent{
            name
            username
            id
        }
    }
}
