mutation editUser($id: ID!, $username: String!, $name: String!, $password: String!) {
    editUser(id: $id, username: $username, name: $name, password: $password) {
        id
        username
        name
        password
    }
}