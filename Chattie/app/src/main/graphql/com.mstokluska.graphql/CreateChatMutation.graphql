mutation createChatMutation($creator: ID!, $recipent: ID!) {
    createChat(creator: $creator, recipent: $recipent) {
        id
        creator{
            id
        }
        recipent{
            id
        }
    }
}